import { useEffect, useState } from 'react';
import api from '../api/client';
import EmptyState from '../components/EmptyState';
export default function Orders() { const [orders, setOrders] = useState([]); useEffect(() => { api.get('/orders/mine').then((r) => setOrders(r.data)); }, []); return <main className="container-page py-10"><h1 className="mb-7 text-3xl font-bold">Order history</h1>{orders.length ? <div className="space-y-4">{orders.map((o) => <article className="card p-5" key={o._id}><div className="flex flex-wrap justify-between gap-2"><div><b>Order #{o._id.slice(-8).toUpperCase()}</b><p className="text-sm text-slate-500">{new Date(o.createdAt).toLocaleDateString()}</p></div><span className="h-fit rounded-full bg-brand-50 px-3 py-1 text-sm font-medium capitalize text-brand-600">{o.status}</span></div><div className="mt-4 flex flex-wrap gap-3">{o.items.map((item) => <div className="flex items-center gap-2 rounded-lg bg-slate-50 p-2" key={item.product}><img src={item.image} className="h-10 w-10 rounded object-cover"/><span className="text-sm">{item.name} × {item.quantity}</span></div>)}</div><p className="mt-4 border-t pt-3 text-right font-bold">Total: ₹{o.totalAmount.toLocaleString('en-IN')}</p></article>)}</div> : <EmptyState title="No orders yet" text="Completed purchases will appear here."/>}</main>; }

